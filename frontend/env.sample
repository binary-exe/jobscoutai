# Frontend Environment Variables
# Copy this file to .env.local and fill in your values

# API URL - point to your backend
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Supabase (Auth)
# Create a Supabase project, then copy these from Project Settings → API
# Make sure Email provider is enabled in Authentication → Providers → Email
# Add redirect URLs in Authentication → URL Configuration:
#   - Site URL: https://your-app.vercel.app (or http://localhost:3000 for local)
#   - Redirect URLs: https://your-app.vercel.app/auth/callback, http://localhost:3000/auth/callback
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Site URL (required for metadata)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# For production, use your actual URLs:
# NEXT_PUBLIC_API_URL=https://your-backend.fly.dev/api/v1
# NEXT_PUBLIC_SITE_URL=https://your-app.vercel.app# PostHog Analytics (optional)
# Create a PostHog account at https://app.posthog.com
# Copy your project API key from Project Settings
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com